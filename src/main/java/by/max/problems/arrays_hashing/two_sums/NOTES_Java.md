# Two Sum

## Описание задачи

Дан массив целых чисел`nums`и целевое число`target`. Необходимо найти **индексы двух чисел** в массиве, сумма которых
равна целевому значению`target`.

**Ограничения:**

- Каждый входной массив имеет ровно**одно решение**

- Нельзя использовать один и тот же элемент дважды

- Ответ можно вернуть в любом порядке

**Примеры:**

**Пример 1:**
Ввод: nums = [2, 7, 11, 15], target = 9
Вывод: [0, 1]
Объяснение: nums[0] + nums[1] = 2 + 7 = 9

**Пример 2:**
Ввод: nums = [3, 2, 4], target = 6
Вывод: [1, 2]
Объяснение: nums[1] + nums[2] = 2 + 4 = 6

## Мышление вслух

1. **Наивный подход:**Проверить все возможные пары элементов с помощью вложенных циклов.

    - **Проблема**: Временная сложность O(n²) неприемлема для больших массивов (до 10⁴ элементов).

2. **Оптимизация с сортировкой**:Отсортировать массив и использовать два указателя.

    - **Проблема**: После сортировки теряются оригинальные индексы. Можно хранить пары (значение, индекс), но это
      усложняет решение и увеличивает сложность до O(n log n).

3. **Ключевое наблюдение**: Для каждого элемента`nums[i]`нужно быстро проверить, есть ли в массиве число
   `target - nums[i]`.

    - **Инсайт:**Для проверки существования элемента за O(1) идеально подходит`HashMap`.

4. **Паттерн:**"Хеш-таблица для поиска комплемента" — один из фундаментальных паттернов в задачах на массивы.

## Выбранный подход

**Алгоритм: Однопроходная хеш-таблица**

**Основная идея**: Использовать`HashMap`для хранения пар "число → его индекс". Для каждого текущего элемента вычисляем
недостающее значение (комплемент) и проверяем, встречалось ли оно ранее.

**Пошаговое описание:**

1. **Инициализация**: Создаем пустой`HashMap<Integer, Integer>`, где ключ — число из массива, значение — его индекс.

2. **Итерация по массиву**: Проходим по массиву один раз слева направо:

    - Для текущего элемента`nums[i]`вычисляем`complement = target - nums[i]`

    - Проверяем, содержится ли`complement`в`HashMap`:

        - **Если да**: Задача решена! Возвращаем массив`[map.get(complement), i]`

        - **Если нет**: Добавляем текущий элемент в хеш-таблицу:`map.put(nums[i], i)`

3. **Завершение**: Поскольку гарантировано существование решения, код всегда найдет ответ до конца цикла.

**Визуализация для`nums = [2, 7, 11, 15]`,`target = 9`:**

```text
Итерация 1: i = 0, nums[0] = 2
  complement = 9 - 2 = 7
  7 нет в map → добавляем {2:0}

Итерация 2: i = 1, nums[1] = 7
  complement = 9 - 7 = 2
  2 есть в map (индекс 0) → возвращаем [0, 1]
```

## Анализ сложности

### Временная сложность (Time Complexity): **O(n)**

- Проходим по массиву**ровно один раз**(n итераций)

- Каждая операция с`HashMap`(`containsKey`,`get`,`put`) выполняется за **O(1)** в среднем случае

- **Итого:**O(n) × O(1) = O(n)

### Пространственная сложность (Space Complexity): **O(n)**

- В худшем случае храним n-1 элементов в`HashMap`(если решение найдется на последней итерации)

- **Итого**: O(n) дополнительной памяти

## 🔄 Альтернативные подходы

### 1. Двухпроходная хеш-таблица

```java
public int[] twoSumTwoPass(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();

    // Первый проход: заполняем хеш-таблицу
    for (int i = 0; i < nums.length; i++) {
        map.put(nums[i], i);
    }

    // Второй проход: ищем пару
    for (int i = 0; i < nums.length; i++) {
        int complement = target - nums[i];
        if (map.containsKey(complement) && map.get(complement) != i) {
            return new int[]{i, map.get(complement)};
        }
    }
    return new int[]{};
}
```

Плюсы: Более понятная логика
Минусы: Два прохода вместо одного, проблемы с дубликатами (последний дубликат перезаписывает индекс)

### 2. Метод двух указателей (требует сортировки)

```java
public int[] twoSumTwoPointers(int[] nums, int target) {
    // Создаем массив пар (значение, оригинальный индекс)
    int[][] indexedNums = new int[nums.length][2];
    for (int i = 0; i < nums.length; i++) {
        indexedNums[i][0] = nums[i];
        indexedNums[i][1] = i;
    }

    // Сортируем по значению
    Arrays.sort(indexedNums, (a, b) -> Integer.compare(a[0], b[0]));

    int left = 0, right = nums.length - 1;
    while (left < right) {
        int sum = indexedNums[left][0] + indexedNums[right][0];
        if (sum == target) {
            return new int[]{indexedNums[left][1], indexedNums[right][1]};
        } else if (sum < target) {
            left++;
        } else {
            right--;
        }
    }
    return new int[]{};
}
```

Плюсы: O(1) дополнительной памяти (если не считать массив пар)
Минусы: O(n log n) из-за сортировки, более сложная реализация

### 3. Полный перебор (Brute Force)

```java
public int[] twoSumBruteForce(int[] nums, int target) {
    for (int i = 0; i < nums.length; i++) {
        for (int j = i + 1; j < nums.length; j++) {
            if (nums[i] + nums[j] == target) {
                return new int[]{i, j};
            }
        }
    }
    return new int[]{};
}
```

**Плюсы**: O(1) дополнительной памяти, простая реализация  
**Минусы**: O(n²) времени — неприемлемо для больших массивов

## Сравнение подходов

| Подход                     | Время      | Память        | Сложность | Рекомендация               |
|----------------------------|------------|---------------|-----------|----------------------------|
| **Однопроходная HashMap**  | **O(n)**   | O(n)          | Низкая    | **Наилучший выбор**        |
| Двухпроходная HashMap      | O(n)       | O(n)          | Низкая    | Хорошая альтернатива       |
| Два указателя + сортировка | O(n log n) | O(n) или O(1) | Средняя   | Если важна экономия памяти |
| Полный перебор             | O(n²)      | O(1)          | Высокая   | Только для обучения        |

## Ключевые выводы

1. **Паттерн "HashMap для поиска комплемента"**— фундаментальный паттерн для задач на поиск пар в массивах.

2. **Компромисс время-память**: HashMap дает оптимальное время O(n) ценой O(n) дополнительной памяти.

3. **Однопроходный алгоритм** предпочтительнее двухпроходного:

    - Экономит один проход по массиву

    - Избегает проблем с дубликатами (не нужно проверять`map.get(complement) != i`)

4. **Важность выбора структур данных:**

    - `HashMap`обеспечивает O(1) поиск по ключу

    - В Java лучше использовать`HashMap`напрямую, а не интерфейс`Map`для производительности

5. **Условие "ровно одно решение"** упрощает реализацию — не нужна обработка случаев с несколькими решениями.

6. **Java-специфичные моменты:**

    - Использование`HashMap<Integer, Integer>`для примитивных типов (автоупаковка)

    - Методы`containsKey()`и`get()`для безопасного поиска

    - Возврат`new int[]{}`как заглушки в конце метода